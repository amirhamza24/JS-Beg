// object
const student = {
    names : 'Unknown',
    age: 35,
    profession: 'student',

}
const {age, profession, names} = student;
console.log(age, profession, names);


// array
const [first, another] = [44, 99, 88, 66];
console.log(first, another);

const nam = ['Sojib', 'Rakib', 'Roni', 'Juwel'];
const [firstName, secondName,thirdName] = nam;
console.log(thirdName, firstName);