const name = "Shakib";
const val = `Hello, ${name}`;

console.log(val);

const person = {
    name: "Rakib",
    age: 24,
}

console.log(`My name is ${person.name} and I am ${person.age} years old`);